// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "Blue.application"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
